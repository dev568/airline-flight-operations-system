apiVersion: v1
kind: Service
metadata:
  name: operations-service
  namespace: airline-operations
spec:
  selector:
    app: operations-service
  ports:
  - protocol: TCP
    port: 8083
    targetPort: 8083
  type: ClusterIP
